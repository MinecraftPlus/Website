<!DOCTYPE html>
<html class="no-js">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no" />

    <title>{% if meta.title %}{{ meta.title }}&nbsp|&nbsp{% endif %}{{ site_title }}</title>
    {% if meta.description %}
        <meta name="description" content="{{ meta.description|striptags }}" />
    {% endif %}
    {% if meta.robots %}
        <meta name="robots" content="{{ meta.robots }}" />
    {% endif %}

    {% if current_page %}
        <link rel="canonical" href="{{ current_page.url }}" />
    {% endif %}

    <link rel="stylesheet" href="{{ theme_url }}/css/style.css" type="text/css" />
    <link rel="stylesheet" href="{{ theme_url }}/css/droidsans.css" type="text/css" />
    <link rel="stylesheet" href="{{ theme_url }}/css/fontello.css" type="text/css" />

	<script async src="https://www.googletagmanager.com/gtag/js?id=G-2T18T0K2B9"></script>
	<script>
	  window.dataLayer = window.dataLayer || [];function gtag(){dataLayer.push(arguments);}gtag('js', new Date());gtag('config', 'G-2T18T0K2B9');
	</script>
</head>
<body{% if config.theme_config.widescreen %} class="widescreen"{% endif %}>

    {% include 'header.twig' %}

    <div id="main" role="main">
        <div class="container">
            {{ content }}

			<h1>Aktualizacje wydane</h1>
			<p><i>Lista aktualizacji, które zostały zaimplementowane do MC+</i></p>
			{% if not pages("updates/released") is empty %}
				{% for page in pages("updates/released")|sort_by("time")|reverse if not page.hidden %}
					<div class="post">
						<h3><a href="{{ page.url }}">{{ page.title }}</a></h3>{{ page.date }}
						<p class="date">{{ page.description }}</p>
					</div>
				{% endfor %}
			{% else %}
				<div class="post">
					<h4><a href="">Brak aktualizacji do wyświetlenia</a></h4>
				</div>
			{% endif %}

			<h1>Aktualizacje zaplanowane</h1>
			<p><i>Lista aktualizacji, które zostaną* zaimplementowane do MC+</i></br>
			<span style="font-size: 8pt;">* Planowane aktualizacje nie mają określonych konkretnych ram czasowych, dlatego na liście posortowane są alfabetycznie.</p>
			{% if not pages("updates/planned") is empty %}
				{% for page in pages("updates/planned")|sort_by("title") if not page.hidden %}
					<div class="post">
						<h3><a href="{{ page.url }}">{{ page.title }}</a></h3>{{ page.date }}
						<p class="date">{{ page.description }}</p>
					</div>
				{% endfor %}
			{% else %}
				<div class="post">
					<h4><a href="">Brak aktualizacji do wyświetlenia</a></h4>
				</div>
			{% endif %}
			
			<h1>Aktualizacje proponowane</h1>
			<p><i>Lista aktualizacji, które mogą zostać zaimplementowane do MC+</i></p>
			{% if not pages("updates/proposed") is empty %}
				{% for page in pages("updates/proposed")|sort_by("title") if not page.hidden %}
					<div class="post">
						<h3><a href="{{ page.url }}">{{ page.title }}</a></h3>{{ page.date }}
						<p class="date">{{ page.description }}</p>
					</div>
				{% endfor %}
			{% else %}
				<div class="post">
					<h4><a href="">Brak aktualizacji do wyświetlenia</a></h4>
				</div>
			{% endif %}
			
        </div>
    </div>
	
	{% include 'footer.twig' %}

    <script src="{{ theme_url }}/js/modernizr-3.3.1-custom.min.js" type="text/javascript"></script>
    <script src="{{ theme_url }}/js/utils.js" type="text/javascript"></script>
    <script src="{{ theme_url }}/js/pico.js" type="text/javascript"></script>

</body>
</html>
